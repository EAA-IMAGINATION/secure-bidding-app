#!/bin/sh
# Runs BEFORE the commit object is created — strips Copilot/Cursor (and other AI) trailers.
# Enable: git config core.hooksPath .githooks

. "$(dirname "$0")/strip-ai-trailers.sh"
strip_ai_trailers "$1"
