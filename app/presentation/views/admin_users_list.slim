div class="row justify-content-center mt-4"
  div class="col-lg-11"
    a href="/" class="btn btn-outline-secondary mb-3" Back to Home

    h2.mb-4 Users

    - if users && users.size > 0
      div class="table-responsive"
        table class="table table-striped table-hover"
          thead class="table-dark"
            tr
              th Username
              th Email
              th Roles
              th Actions
          tbody
            - users.each do |user|
              tr
                td
                  = user['username']
                td
                  - if user['email'] && !user['email'].to_s.strip.empty?
                    = user['email']
                    - if user['email_verified'] || user['email_verified'] == 'true'
                      span.badge.bg-success.ms-2 Verified
                    - else
                      span.badge.bg-secondary.ms-2 Unverified
                  - else
                    | —
                td
                  - if user['system_roles'] && user['system_roles'].size > 0
                    - user['system_roles'].each do |role|
                      span class="badge bg-info me-1"
                        = role
                  - elsif user['system_role'] && !user['system_role'].to_s.strip.empty?
                    span class="badge bg-info me-1"
                      = user['system_role']
                  - else
                    span class="text-muted" No roles assigned
                td
                  div class="d-flex gap-1"
                    a href="/admin/users/#{user['id']}" class="btn btn-sm btn-info" View
                    a href="/admin/users/#{user['id']}/roles" class="btn btn-sm btn-primary" Promote
    - else
      div class="alert alert-warning" No users found
